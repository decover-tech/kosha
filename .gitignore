# Rust build artifacts. Cargo.lock is intentionally committed: this workspace
# ships a binary and the lockfile is the reproducible-build contract.
/target
**/*.rs.bk

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp

# Python bytecode
**/__pycache__/
*.pyc

# Local dev volumes (docker-compose)
/volumes
