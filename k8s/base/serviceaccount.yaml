# IRSA role ARN is per-environment (different account/role per cluster) —
# layered in by each overlay's serviceaccount-patch.yaml.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kosha
  namespace: kosha
