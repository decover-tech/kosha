# Role ARN follows the same {name_prefix}-kosha convention confirmed live
# on staging (arn:...:role/decoverai-stage-kosha) — NOT independently
# verified against the prod account (992382824254; this session's AWS
# credentials only reach the nonprod account). Confirm before first deploy.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kosha
  namespace: kosha
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::992382824254:role/decoverai-prod-kosha
