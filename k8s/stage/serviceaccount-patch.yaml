# IRSA role ARN is NOT committed here — the deploy action annotates the
# ServiceAccount at apply time from GitHub Environment secrets (see
# .github/actions/deploy-eks).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kosha
  namespace: kosha
